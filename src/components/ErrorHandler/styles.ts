import {View} from 'react-native';
import styled from 'styled-components';

export const ErrorHandlerMainWrapper = styled(View)`
  flex: 1;
  justify-content: center;
  align-content: center;
  padding-right: 12px;
  padding-left: 12px;
  background-color: #1a8fe3;
`;
